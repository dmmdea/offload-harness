# Normalize line endings. The repo stores LF; shell scripts MUST stay LF
# (CRLF in a shebang/command breaks bash on Linux/macOS).
* text=auto eol=lf
*.sh  text eol=lf
*.go  text eol=lf
*.mod text eol=lf
*.sum text eol=lf
