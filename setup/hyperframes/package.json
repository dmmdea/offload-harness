{
  "name": "offload-harness-hyperframes",
  "version": "0.0.0",
  "private": true,
  "description": "Pinned, project-local HyperFrames install for the offload_compose_video lane (ADR 0059). Installed with `npm ci --ignore-scripts` from the committed lockfile; never installed globally.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "hyperframes": "0.8.61"
  },
  "allowScripts": {
    "esbuild@0.25.12": true
  }
}
