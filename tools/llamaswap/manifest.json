{
  "manifest_version": "0.3",
  "name": "llamaswap-pp-mcp",
  "display_name": "llama-swap",
  "version": "4.30.2",
  "description": "The llama-swap operations console — durable history, drain-aware control, and measurement commands that structurally cannot produce the errors operators actually make.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/llamaswap-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/llamaswap-pp-mcp",
      "args": [],
      "env": {
        "PRINTING_PRESS_CLIENT_PROFILE": "${user_config.printing_press_client_profile}"
      }
    }
  },
  "user_config": {
    "printing_press_client_profile": {
      "type": "string",
      "title": "Client profile",
      "description": "Binds the MCP server to an existing tenant-gated Printing Press client profile.",
      "required": true
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  }
}
